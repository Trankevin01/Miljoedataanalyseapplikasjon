Vi har ikke brukt list comprehensions, men et eksempel på hvordan Vi kunne ha brukt list comprehensions for å effektivt 
filtrere eller transformere dataene i én linje. For eksempel, hvis vi hadde en liste over tall og ønsket å lage en ny liste 
med kvadratet av alle tallene som er større enn 10, kunne vi skrevet:

result = [x**2 for x in numbers if x > 10]

Dette ville ha gitt oss en ny liste der hvert element er kvadratet av tallene som oppfyller betingelsen x > 10. 
List comprehensions er en kompakt og lesbar måte å kombinere løkker og betingelser på, men vi har valgt å bruke vanlige 
løkker i stedet for å gjøre koden mer eksplisitt og lettere å lese.